async changeStatus(item){
                let query = {
                    id:item.id,
                    status:item.status
                }
                let options = {
                    params: query,
                    method: put
                };
                let url = this.ajax_url + '/change-status';
                await vaah().ajax(
                    url,
                    this.afterChangeStatus,
                    options
                );
            },
